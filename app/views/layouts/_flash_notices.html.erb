<div class="noticesWrapper col-md-12 col-xs-12 col-sm-12">
  <% flash.each do |name, msg| %>
    <div class="alert alert-<%= name == 'notice' ? "success" : "danger" %>">
      <a class="close" data-dismiss="alert"><i class="fa fa-times"></i></a>
      <%= msg %>
    </div>
  <% end %>
</div>
