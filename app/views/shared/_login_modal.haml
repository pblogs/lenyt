//Modal login
#myModal1.modal.fade{aria: {hidden: true, labelledby: 'myModalLabel'}, role: 'dialog', tabindex: '-1'}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{type: 'button', aria: {label: 'Close'}, data: {dismiss: 'modal'}}= raw("<span aria-hidden='true'>&times;</span>")
      .modal-body
        .form-sign
          .social-btns
            = link_to "sign Up with Facebook", '#', class: 'face'
            = link_to "sign Up with Google", '#', class: 'google'
          .or= 'or'
          .log-inp
            == <input type="text" placeholder="Username" />
            == <input type="password" placeholder="Password" />
            == <input type="submit" value="Sign Up" />
        %span.sign= raw("By signing up, I agree to Lenyt’s<br /> User Agreement and Terms of Services.")
        %p.already
          = "Already a member? "
          = link_to 'Log In', '#'
