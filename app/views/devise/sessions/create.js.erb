<% if resource.valid? %>
location.reload();
console.log('Valid');
<% else %>
 console.log('InValid');
$('.alert.alert-danger.alert-block').remove();
$(document).find("#user_login_modal").prepend("<%= j(render 'shared/error_messages', object: resource) %>");
<% end %>
