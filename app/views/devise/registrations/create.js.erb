<% if resource.valid? %>
  console.log('Valid');
  $('.alert.alert-danger.alert-block').remove();
  // tr = $('.educations_list').find('tr').last();
  // $('<%= j(render(@education)) %>').insertAfter(tr);
  // $("#new_education")[0].reset();
  // $('#education_year').attr('type', '').val('');
  // $('.selectpicker').selectpicker('refresh');
  // $('.file_field').on('change', function(event) {
  //   $(this).closest("form").submit();
  // });
<% else %>
  // console.log('InValid');
  $('.alert.alert-danger.alert-block').remove();
  $(document).find("#user_signup_modal").prepend("<%= j(render 'shared/error_messages', object: resource) %>");
<% end %>
