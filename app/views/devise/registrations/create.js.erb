<% if resource.valid? %>
  location.replace('<%= root_url %>');
  // $('.modal-body').html('<h5>You have to confirm your account before continuing.</h5>');
<% else %>
  $('#error_messages').remove();
  $(document).find("#user_signup_modal").prepend("<%= j(render 'shared/error_messages', object: resource) %>");
<% end %>
