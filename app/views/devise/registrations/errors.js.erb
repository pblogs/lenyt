if($('#flash').length > 0) {
  $('#flash').remove();
  $('.modal-body').prepend("<%= j(render 'shared/error_messages', object: resource) %>");
}
else {
  $('.modal-body').prepend("<%= j(render 'shared/error_messages', object: resource) %>");
}
